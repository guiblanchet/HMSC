#include <RcppArmadillo.h>
#include <Rcpp.h>
#include "condPred.h"

using namespace arma; 
using namespace Rcpp;

// Calculates a prediction conditional on a subset of species.
arma::mat condPred(arma::mat& Y
				   arma::mat& EstModel) {
	
	// Still have to write this part //

	// return result object
	return res;
}
